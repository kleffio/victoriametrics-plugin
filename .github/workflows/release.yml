name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/kleffio/metrics-victoriametrics:${{ steps.version.outputs.version }}
            ghcr.io/kleffio/metrics-victoriametrics:latest

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.KLEFF_APP_ID }}
          private-key: ${{ secrets.KLEFF_APP_PRIVATE_KEY }}
          owner: kleffio
          repositories: plugin-registry

      - name: Open PR to plugin-registry
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            const version = "${{ steps.version.outputs.version }}";
            const branch = `update/metrics-victoriametrics-${version}`;

            const { data: file } = await github.rest.repos.getContent({
              owner: "kleffio",
              repo: "plugin-registry",
              path: "plugins.json",
            });

            const plugins = JSON.parse(Buffer.from(file.content, "base64").toString());
            const idx = plugins.findIndex(p => p.id === "victoriametrics-plugin");
            if (idx !== -1) plugins[idx].version = version;

            const { data: ref } = await github.rest.git.getRef({
              owner: "kleffio",
              repo: "plugin-registry",
              ref: "heads/main",
            });

            try {
              await github.rest.git.createRef({
                owner: "kleffio",
                repo: "plugin-registry",
                ref: `refs/heads/${branch}`,
                sha: ref.object.sha,
              });
            } catch (e) {
              if (e.status === 422) {
                await github.rest.git.updateRef({
                  owner: "kleffio",
                  repo: "plugin-registry",
                  ref: `heads/${branch}`,
                  sha: ref.object.sha,
                  force: true,
                });
              } else throw e;
            }

            await github.rest.repos.createOrUpdateFileContents({
              owner: "kleffio",
              repo: "plugin-registry",
              path: "plugins.json",
              message: `chore: bump victoriametrics-plugin to v${version}`,
              content: Buffer.from(JSON.stringify(plugins, null, 2) + "\n").toString("base64"),
              branch,
              sha: file.sha,
            });

            const { data: pr } = await github.rest.pulls.create({
              owner: "kleffio",
              repo: "plugin-registry",
              title: `chore: bump victoriametrics-plugin to v${version}`,
              head: branch,
              base: "main",
              body: `Automated release PR from \`kleffio/victoriametrics-plugin\` tag \`v${version}\`.`,
            });

            await github.rest.pulls.merge({
              owner: "kleffio",
              repo: "plugin-registry",
              pull_number: pr.number,
              merge_method: "squash",
            });
